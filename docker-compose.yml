version: "3.6"
services: 
    flask:
        build: ./
        depends_on: 
            postgres:
                condition: service_healthy
        env_file:
            - .env
        ports: 
            - "${FLAST_HOST_PORT}:5000"
        restart: always
    postgres:
        image: "postgres:alpine"
        env_file:
            - .env
        ports:
            - "5432:5432"
        healthcheck:
            test: ["CMD-SHELL", "pg_isready -U test -d cached_db"]
            interval: 5s
            timeout: 2s
            retries: 3
        restart: always